$ rtk proxy bun test test/app.test.js -t 'an invalid locked reread invalidates refresh and requires state revalidation'
bun test v1.4.2 (744846f84)

test/app.test.js:
3594 |       rt.readState = () => ++reads === 1 ? structuredClone(initialState) : structuredClone(locked);
3595 |       rt.acquireLock = () => () => { released = true; };
3596 |       const report = await execute(parseArgs(["upgrade", "/repo", "--host", "codex", "--refresh-pending"]), rt);
3597 |       const guidance = [report.conflicts.map((item) => item.detail).join("\n"), report.nextActions.join("\n")].join("\n");
3598 |       expect(report.conflicts.map((item) => item.code)).toContain("STATE_CONFLICT");
3599 |       expect(guidance).toContain("inspect and validate the saved Devkit state");
                              ^
error: expect(received).toContain(expected)

Expected to contain: "inspect and validate the saved Devkit state"
Received: "Invalid devkit state structure. Resolve the reported native conflict, then complete the recorded plan with hoklims-devkit setup /repo --host codex.\nResolve the reported native conflict, then complete the recorded plan with hoklims-devkit setup /repo --host codex"

      at <anonymous> (/tmp/claude-1000/-home-laegel--claude-session/e90c724e-4ef0-48ad-8599-172e694fc5f1/scratchpad/w-E-recovery/test/app.test.js:3599:24)
(fail) public CLI > an invalid locked reread invalidates refresh and requires state revalidation [9.34ms]

 0 pass
 158 filtered out
 1 fail
 2 expect() calls
Ran 1 test across 1 file. [64.00ms]
EXIT=1
