$ rtk proxy bun test test/app.test.js -t 'an unverifiable early state makes the current retry explicitly conditional'
bun test v1.4.2 (744846f84)

test/app.test.js:
289 |     rt.which = (name) => ["codex", "claude"].includes(name) ? `/bin/${name}` : null;
290 |     rt.realpath = () => { throw Object.assign(new Error("profile access denied"), { code: "EACCES" }); };
291 |     const report = await execute(parseArgs(["upgrade", "/repo", "--host", "all", "--refresh-pending"]), rt);
292 |     const guidance = [report.conflicts.map((item) => item.detail).join("\n"), report.nextActions.join("\n")].join("\n");
293 |     expect(report.conflicts.map((item) => item.code)).toContain("BUN_REQUIRED");
294 |     expect(guidance).toContain("inspect and validate the saved Devkit state");
                           ^
error: expect(received).toContain(expected)

Expected to contain: "inspect and validate the saved Devkit state"
Received: "Bun >=1.4 is required. Install Bun >=1.4 and ensure bun and bunx are on PATH, then restore Bun on PATH before running hoklims-devkit upgrade /repo --host all --refresh-pending.\nInstall Bun >=1.4 and ensure bun and bunx are on PATH, then restore Bun on PATH before running hoklims-devkit upgrade /repo --host all --refresh-pending"

      at <anonymous> (/tmp/claude-1000/-home-laegel--claude-session/e90c724e-4ef0-48ad-8599-172e694fc5f1/scratchpad/w-E-recovery/test/app.test.js:294:22)
(fail) public CLI > an unverifiable early state makes the current retry explicitly conditional [7.14ms]

 0 pass
 158 filtered out
 1 fail
 2 expect() calls
Ran 1 test across 1 file. [63.00ms]
EXIT=1
