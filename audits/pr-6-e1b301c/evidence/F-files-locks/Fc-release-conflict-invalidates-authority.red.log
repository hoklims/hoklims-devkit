$ rtk proxy bun test test/app.test.js -t 'lock release ownership conflict|typed state path conflicts stay STATE_CONFLICT at every locked boundary|STATE_CHANGED release failures'
bun test v1.4.2 (744846f84)

test/app.test.js:
3213 |     };
3214 |     const report = await execute(parseArgs(["setup", "/repo", "--host", "codex"]), rt);
3215 |     const guidance = [...report.nextActions, ...report.conflicts.map((item) => item.detail)].join("\n");
3216 |     expect(report.conflicts.map((item) => item.code)).toContain("APPLY_FAILED");
3217 |     expect(report.conflicts.map((item) => item.code)).toContain("STATE_CONFLICT");
3218 |     expect(guidance).toContain("inspect and validate the saved Devkit state");
                            ^
error: expect(received).toContain(expected)

Expected to contain: "inspect and validate the saved Devkit state"
Received: "Resolve the reported native conflict, then complete the recorded plan with hoklims-devkit setup /repo --host codex\nsemctx: Semctx workspace setup: native setup failed. Resolve the reported native conflict, then complete the recorded plan with hoklims-devkit setup /repo --host codex.\nlock ownership changed during failed read. Resolve the reported native conflict, then complete the recorded plan with hoklims-devkit setup /repo --host codex."

      at <anonymous> (/tmp/claude-1000/-home-laegel--claude-session/e90c724e-4ef0-48ad-8599-172e694fc5f1/scratchpad/w-F-files-locks/test/app.test.js:3218:22)
(fail) public CLI > lock release ownership conflict makes recorded-plan recovery conditional [2.21ms]
3942 |     interruptedRelease.acquireLock = () => () => { throw conflict("lock was replaced before release"); };
3943 |     const interrupted = await execute(setupOptions(), interruptedRelease);
3944 |     const guidance = [interrupted.conflicts.map((item) => item.detail).join("\n"), interrupted.nextActions.join("\n")].join("\n");
3945 |     expect(interrupted.conflicts.map((item) => item.code)).toContain("APPLY_FAILED");
3946 |     expect(interrupted.conflicts.map((item) => item.code)).toContain("STATE_CONFLICT");
3947 |     expect(guidance).toContain("inspect and validate the saved Devkit state");
                            ^
error: expect(received).toContain(expected)

Expected to contain: "inspect and validate the saved Devkit state"
Received: "semctx: Semctx workspace setup: simulated native failure. Resolve the reported native conflict, then complete the recorded plan with hoklims-devkit setup /repo --host codex.\nlock was replaced before release. Resolve the reported native conflict, then complete the recorded plan with hoklims-devkit setup /repo --host codex.\nResolve the reported native conflict, then complete the recorded plan with hoklims-devkit setup /repo --host codex"

      at <anonymous> (/tmp/claude-1000/-home-laegel--claude-session/e90c724e-4ef0-48ad-8599-172e694fc5f1/scratchpad/w-F-files-locks/test/app.test.js:3947:22)
(fail) public CLI > typed state path conflicts stay STATE_CONFLICT at every locked boundary [3.90ms]

 1 pass
 156 filtered out
 2 fail
 27 expect() calls
Ran 3 tests across 1 file. [62.00ms]
EXIT=1
